<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/setting_detail_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="20dp">

    <TextView
        android:id="@+id/setting_detail_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:contentDescription="화면 제목 설정 상세"
        android:text="설정 상세"
        android:textSize="26sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/setting_detail_description"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="설정 설명" />

    <LinearLayout
        android:id="@+id/setting_detail_content"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:contentDescription="설정 상세 내용"
        android:orientation="vertical" />

    <Button
        android:id="@+id/back_to_settings_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:contentDescription="설정으로 돌아가기"
        android:text="설정으로 돌아가기" />
</LinearLayout>
